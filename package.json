{
  "name": "micro-react-ant-design",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "keywords": [],
  "license": "ISC",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "biome:lint": "npx @biomejs/biome lint",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "clear": "rm -fr node_modules && rm -fr .pnpm-store && rm -fr pnpm-lock.yaml && pnpm --filter \"./packages-**/**\" clear",
    "boot": "pnpm run clear && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@mt-kit/eslint-config": "^2.3.0",
    "eslint": "^9.34.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "typescript": "^5.6.3"
  }
}
